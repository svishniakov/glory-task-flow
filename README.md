# Glory Taskflow

`Glory Taskflow` помогает готовить задачи для Jira: формулировать, проверять, сокращать и декомпозировать Feature, Task, Research, Spike и Sub-task.

Скилл основан на канонической странице Confluence `Payments-003: Подход к формированию описаний задач`.

## Как запускать

Скилл запускается только явно. Поставь имя скилла в начало сообщения:

```text
$glory-taskflow <запрос>
```

Также работают варианты:

```text
Glory Taskflow <запрос>
glory-taskflow <запрос>
глори таскфлоу <запрос>
```

## Пустой запуск

Если вызвать скилл без текста:

```text
$glory-taskflow
```

он сразу запустит intake wizard и задаст первый вопрос:

```text
Что хотим сделать?
```

Дальше скилл будет по одному собирать недостающие вводные:

- что хотим сделать;
- зачем это нужно;
- какую проблему решаем;
- какой результат ожидаем;
- какие ограничения или открытые вопросы уже известны.

После этого он определит тип задачи и подготовит Jira-ready черновик.

## Примеры использования

Создать задачу из идеи:

```text
$glory-taskflow Сделай Jira task из идеи: нужно добавить fallback на старый источник балансов, если новый provider недоступен.
```

Проверить готовое описание:

```text
$glory-taskflow Проверь описание задачи:
<текст задачи>
```

Сократить длинный текст:

```text
$glory-taskflow Сократи до Jira-ready формата:
<длинный текст>
```

Разбить Feature на задачи:

```text
$glory-taskflow Разбей эту Feature на задачи по 1-3 SP:
<описание feature>
```

Сформировать Research / Spike:

```text
$glory-taskflow Оформи Spike: нужно понять, как сейчас считается available balance и где лучше добавить проверку лимитов.
```

## Что скилл делает

Скилл помогает:

- отличать Feature от Task;
- отделять Research / Spike от implementation-задач;
- выделять Context, Goal, Scope, Out of Scope, Acceptance Criteria и Definition of Done;
- убирать лишнюю теорию из описания;
- находить размытый scope;
- разбивать крупную работу на маленькие задачи;
- писать формулировки, понятные backend, frontend, QA, product и PM.

Скилл не придумывает факты, API, поля, сроки, бизнес-правила и архитектурные решения. Если данных не хватает, он задаёт короткие вопросы или помечает пробелы как `TODO` / `Assumption`.

## Установка через AI-агента

Не нужно ставить скилл вручную. Скорми prompt ниже AI-агенту: Codex, Claude Code или другому агенту, который умеет работать с файлами.

Агент должен установить скилл глобально, не в папку проекта. Так он будет доступен всем AI-агентам этого пользователя, которые читают глобальные skills.

Для Codex глобальный путь:

```text
~/.codex/skills/glory-taskflow
```

Для Claude Code или другого агента используй его глобальную папку skills. Если агент поддерживает несколько хостов, попроси его установить копию в каждый глобальный каталог, который он реально использует.

## Prompt для установки

```text
Установи глобальный skill `glory-taskflow` для текущего пользователя.

Требования:

1. Установи skill глобально, а не в проект:
   - для Codex: `~/.codex/skills/glory-taskflow`;
   - для Claude Code: глобальная папка skills Claude Code, если она доступна;
   - для других AI-агентов: их глобальная папка skills.

2. Создай структуру:
   - `SKILL.md`;
   - `agents/openai.yaml`;
   - `references/task-description-prompt.md`;
   - `README.md`.

3. Содержимое skill должно быть основано на canonical source:
   - Confluence page: `Payments-003: Подход к формированию описаний задач`;
   - page ID: `2079162374`;
   - site: `glorybet.atlassian.net`;
   - URL: `https://glorybet.atlassian.net/wiki/spaces/Arch/pages/2079162374/Payments-003`.

4. Используй страницу как источник истины. Если у агента нет доступа к странице, попроси у пользователя текст страницы или экспорт.

5. Skill должен запускаться только явно:
   - `$glory-taskflow`;
   - `Glory Taskflow`;
   - `glory-taskflow`;
   - `глори таскфлоу`.

6. Если пользователь вызывает только `$glory-taskflow` без деталей, запускай intake wizard и первым вопросом спрашивай:
   `Что хотим сделать?`

7. Skill должен помогать:
   - создавать Jira-ready задачи;
   - проверять описания задач;
   - сокращать длинные тексты до рабочего формата;
   - декомпозировать Feature на Task/Sub-task;
   - оформлять Research / Spike;
   - выделять Context, Goal, Scope, Out of Scope, Acceptance Criteria и Definition of Done.

8. Skill не должен выдумывать:
   - бизнес-правила;
   - API-контракты;
   - названия полей;
   - сроки;
   - зависимости;
   - ограничения;
   - архитектурные решения;
   - договорённости между командами.

9. Не добавляй внешние инструменты в metadata скилла. Обновления скилла должны приходить из GitHub.

10. После установки проверь:
    - что `SKILL.md` валиден;
    - что YAML-файлы парсятся;
    - что пустой запуск `$glory-taskflow` начинает wizard с вопроса `Что хотим сделать?`.

11. Не записывай tokens, cookies, API keys и другие секреты в файлы skill, README, project memory или ответ пользователю.
```

## Проверка после установки

Минимальная проверка:

```text
$glory-taskflow
```

Ожидаемый ответ:

```text
Что хотим сделать?
```

Проверка с задачей:

```text
$glory-taskflow Сделай Task: нужно добавить логирование ошибок при неуспешной синхронизации баланса.
```

Ожидаемый результат: компактное Jira-ready описание с Context, Goal, Scope, Acceptance Criteria и Definition of Done.
